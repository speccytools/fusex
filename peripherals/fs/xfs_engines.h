#pragma once

#include "xfs.h"

extern struct xfs_engine_t http_engine;
extern struct xfs_engine_t https_engine;
extern struct xfs_engine_t xfs_ram_engine;
extern const struct xfs_engine_t xfs_romfs_engine;
extern const struct xfs_engine_t xfs_overlay_engine;
